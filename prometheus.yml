global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'blogr-api-service'
    metrics_path: '/metrics'
    scheme: https
    http_headers:
      X-Metrics-Token:
        vakues: ['${METRICS_TOKEN}']
    static_configs:
      - targets: ['${BACKEND_INTERNAL_URL}']
